#include <stdio.h>

int main() {
    int num = 7, i, flag = 1;

    for(i = 2; i <= num/2; i++) {
        if(num % i == 0) {
            flag = 0;
            break;
        }
    }

    if(flag == 1)
        printf("Prime\n");
    else
        printf("Not Prime\n");

    return 0;
}

OUTPUT:
Prime
